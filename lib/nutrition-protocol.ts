import type { Goal, MacroNormsPerKg, Sex } from "./types";

/** Діапазони г/кг за практичним протоколом (Ліндовер, Гаманюк, Литвиненко) */
export const PROTOCOL_MACRO_RANGES = {
  female: {
    protein: { min: 1.3, max: 1.8 },
    fat: { min: 1.0, max: 1.0 },
    carbs: { min: 1.5, max: 2.0 },
  },
  male: {
    protein: { min: 1.8, max: 2.8 },
    fat: { min: 1.0, max: 1.5 },
    carbs: { min: 2.0, max: 3.0 },
  },
} as const;

/** Текст правил для системного промпту ШІ */
export const NUTRITION_PROTOCOL_PROMPT = `ПРОТОКОЛ РОЗРАХУНКУ БЖВ (суворо):
- Для дівчат: білок 1.3–1.8 г/кг, жири 1.0 г/кг, вуглеводи 1.5–2.0 г/кг.
- Для хлопців: білок 1.8–2.8 г/кг, жири 1.0–1.5 г/кг, вуглеводи 2.0–3.0 г/кг.
- Обирай значення в діапазоні залежно від цілі (схуднення → нижня межа білка/вуглеводів за потреби; набір маси → верхня), активності та тренування дня.
- Вага продуктів — СУВОРО у сухому/сирому вигляді (100 г сухого рису, 150 г сирого філе).
- Овочі (клітковина) НЕ зараховуй у калораж БЖВ основних страв, але обов'язково додавай до обіду та/або вечері як джерело клітковини.
- Продукти максимально різноманітні впродовж тижня — повний амінокислотний та мікронутрієнтний профіль.

ЖОРСТКІ ОБМЕЖЕННЯ ПРОДУКТІВ:
- Повністю виключити: цукор, солодощі, біле магазинне борошно, випічку на дріжджах і білому борошні.
- Дозволено: бездріжджовий хліб, хліб з висівками, макарони твердих сортів, гречка, рис, овес, перловка, кіноа, сочевиця.

БІОХІМІЧНІ ПРАВИЛА (Ліндовер / Гаманюк):
- Порція білка за один прийом — не більше 30–50 г (індивідуальне засвоєння).
- Сніданок: усунення нічного катаболізму легкозасвоюваним білком. Визнач тип сніданку (білково-жировий або вуглеводно-білковий) і поясни в обґрунтуванні.
- Омега-3, корисні жири (авокадо, оливкова олія, жовтки) — обов'язкові для гормонального здоров'я та клітинних мембран.
- Пріоритет тваринним білкам (повний амінопрофіль). При непереносимості лактози — ізоляти або безлактозні альтернативи.`;

export const MENU_JUSTIFICATION_PROMPT = `ПОЛЕ menu_justification (ОБОВ'ЯЗКОВЕ для кожного дня):
Ти — глибоко освічений нутриціолог-біохімік. Після формування меню напиши детальний текст у полі menu_justification (500–900 слів, розмовний але професійний стиль, українською).

Обов'язково розкрий:
1. Чому обрано саме таке співвідношення БЖВ під ціль клієнта (схуднення / набір маси / підтримка).
2. Навіщо додані конкретні продукти (напр.: «Додав горіхи та оливкову олію у вечерю — жири потрібні для синтезу статевих гормонів, відновлення вночі та терморегуляції»).
3. Транспортна та ферментативна функція білка: альбуміни як транспорт вітамінів, імунітет, ферменти підшлункової залози.
4. Питний режим: ~300 мл чистої води за 15–20 хв до їжі; 1–2 л під час тренувань; гідратація для виведення аміаку/кетонів нирками і нормального жироспалювання в печінці.
5. Попередження про шлунок: для засвоєння меню потрібна адекватна кислотність шлунка, інакше білок не розщепиться і може ферментувати в кишечнику.

Не дублюй дослівно список страв — пояснюй ЛОГІКУ та біохімію.`;

function pickInRange(min: number, max: number, bias: "low" | "mid" | "high"): number {
  if (bias === "low") return min;
  if (bias === "high") return max;
  return Math.round(((min + max) / 2) * 10) / 10;
}

/** Норми г/кг за протоколом з урахуванням статі та цілі */
export function protocolMacroNormsPerKg(
  sex: Sex,
  goal: Goal,
  activityLevel: number
): MacroNormsPerKg {
  const ranges = PROTOCOL_MACRO_RANGES[sex === "male" ? "male" : "female"];

  let proteinBias: "low" | "mid" | "high" = "mid";
  let carbsBias: "low" | "mid" | "high" = "mid";
  let fat = pickInRange(ranges.fat.min, ranges.fat.max, "mid");

  if (goal === "weight_loss") {
    proteinBias = "high";
    carbsBias = "low";
  } else if (goal === "muscle_gain") {
    proteinBias = "high";
    carbsBias = "high";
  }

  if (activityLevel >= 1.55) carbsBias = carbsBias === "low" ? "mid" : "high";
  if (activityLevel >= 1.725 && sex === "male") proteinBias = "high";

  return {
    protein: pickInRange(ranges.protein.min, ranges.protein.max, proteinBias),
    fat,
    carbs: pickInRange(ranges.carbs.min, ranges.carbs.max, carbsBias),
  };
}
